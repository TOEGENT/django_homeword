# 🚀 Проект: Starships (Django Homework)

Это учебный проект на Django, созданный в рамках домашнего задания. Проект представляет собой простое веб-приложение для работы с данными о космических кораблях (`starships`).

---

## 📁 Структура проекта

```
django_homework/
├── .venv/                  # Виртуальное окружение Python
├── swsite/                 # Основной проект Django
│   ├── starships/          # Приложение для управления кораблями
│   │   ├── migrations/     # Миграции базы данных
│   │   ├── templates/      # Шаблоны HTML
│   │   ├── __init__.py
│   │   ├── admin.py        # Настройки админки
│   │   ├── apps.py         # Конфигурация приложения
│   │   ├── models.py       # Модели данных
│   │   ├── tests.py        # Тесты
│   │   ├── urls.py         # URL-маршруты приложения
│   │   └── views.py        # Логика обработки запросов
│   ├── swsite/             # Настройки проекта
│   │   ├── __init__.py
│   │   ├── settings.py     # Настройки Django
│   │   ├── urls.py         # Главные URL-маршруты
│   │   └── wsgi.py         # WSGI-конфигурация
│   └── manage.py           # Утилита управления проектом
└── README.md               # Этот файл
```

---

## ⚙️ Как запустить проект

### 1. Клонируйте репозиторий

```bash
git clone <ваш_репозиторий>
cd django_homework
```

### 2. Активируйте виртуальное окружение

```bash
# Windows
.venv\Scripts\activate

# macOS / Linux
source .venv/bin/activate
```

> 💡 Если `.venv` не создан — создайте его:
> ```bash
> python -m venv .venv
> ```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

> ⚠️ Если файла `requirements.txt` нет — создайте его командой:
> ```bash
> pip freeze > requirements.txt
> ```
> или установите Django вручную:
> ```bash
> pip install django
> ```

### 4. Примените миграции

```bash
python manage.py migrate
```

### 5. Запустите сервер разработки

```bash
python manage.py runserver
```

Перейдите по адресу: [http://localhost:8000](http://localhost:8000)

---

## 🌐 Доступные URL

- **Главная страница**: `/`
- **Админка**: `/admin/` *(логин и пароль нужно создать командой `python manage.py createsuperuser`)*
- **API/страницы кораблей**: `/starships/` *(если настроено в `starships/urls.py`)*

---

## 🧪 Тестирование

Запуск тестов:

```bash
python manage.py test starships
```

---

## 📝 Примечания

- Проект использует SQLite как базу данных — подходит для обучения и небольших проектов.
- Все модели, представления и шаблоны находятся в приложении `starships`.
- Для расширения функциональности можно добавить новые модели (например, `Crew`, `Planet`) или API через DRF.

---

## 📬 Поддержка

Если у вас есть вопросы или предложения — пишите в issues!

---

✅ *Проект готов к изучению и доработке.*

---

> ✨ **Создано с ❤️ для обучения Django**  
> 📅 Последнее обновление: 11 ноября 2025 г.

